<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".AppConfig.Application.AutyApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo_app"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo_app"
        android:supportsRtl="true"
        android:theme="@style/Theme.Auty"
        tools:targetApi="31">
        <activity
            android:name=".UI.Activity.authen.LoginActivityScreen2"
            android:exported="false" />
        <activity
            android:name=".UI.Activity.authen.RegisterActivity"
            android:exported="true" />
        <activity
            android:name=".UI.Activity.authen.LoginActivity"
            android:exported="true" />
        <activity
            android:name=".UI.Activity.start.StartActivity"
            android:exported="false" />
        <activity
            android:name=".UI.Activity.splash.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".UI.Activity.main.MainActivity"
            android:exported="true" />
    </application>

</manifest>